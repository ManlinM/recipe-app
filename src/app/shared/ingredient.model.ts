export class Ingredient{
//how the ingredient should contain
// public name: string;
// public amount:number;
//Instead of doing the above, can just add public in front of the param
constructor(public name:string,public amount:number){

}
}